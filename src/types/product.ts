// src/types/product.ts// Тип для товара из APIexport interface Product {    id: number;    title: string;    description: string;    price: number;    discountPercentage: number;    rating: number;    stock: number;    brand: string;    category: string;    thumbnail: string;    images: string[];}// Тип для ответа API со списком товаровexport interface ProductsResponse {    products: Product[];    total: number;    skip: number;    limit: number;}// Тип для ответа авторизацииexport interface AuthResponse {    id: number;    username: string;    email: string;    firstName: string;    lastName: string;    gender: string;    image: string;    token: string;}// Тип для формы добавления товара (локальный)export interface AddProductFormData {    title: string;    price: number;    brand: string;    sku: string;}// Тип для нового товара (отправка на API)export interface NewProduct {    title: string;    price: number;    brand: string;    sku?: string;    description?: string;    category?: string;}