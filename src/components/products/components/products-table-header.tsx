interface ProductsTableHeaderProps {    isAllSelected: boolean;    totalItems: number;    onSelectAll: () => void;    sortBy: 'price' | 'rating' | null;    sortOrder: 'asc' | 'desc';    onSort: (field: 'price' | 'rating') => void;}export const ProductsTableHeader = ({    isAllSelected,    totalItems,    onSelectAll,    sortBy,    sortOrder,    onSort,}: ProductsTableHeaderProps) => {    return (        <thead className="border-b border-gray-200">            <tr className="text-sm font-medium" style={{ color: '#B2B3B9', height: '73px' }}>                <th className="w-[60px] text-center">                    <div className="flex items-center justify-center">                        <div                            onClick={onSelectAll}                            className={`w-[22px] h-[22px] rounded cursor-pointer flex items-center justify-center ${                                isAllSelected && totalItems > 0                                    ? 'bg-[#3C538E]'                                    : 'border-2 border-gray-300 bg-white'                            }`}                        >                            {isAllSelected && totalItems > 0 && (                                <svg                                    className="w-[14px] h-[14px] text-white"                                    fill="none"                                    viewBox="0 0 24 24"                                    stroke="currentColor"                                    strokeWidth="3"                                >                                    <path                                        strokeLinecap="round"                                        strokeLinejoin="round"                                        d="M5 13l4 4L19 7"                                    />                                </svg>                            )}                        </div>                    </div>                </th>                <th className="w-[calc(40%-60px)] text-left">Наименование</th>                <th className="w-[15%] text-center">Вендор</th>                <th className="w-[15%] text-center">Артикул</th>                <th className="w-[10%] text-center">                    <button                        onClick={() => onSort('rating')}                        className="flex items-center justify-center gap-1 hover:text-gray-700 w-full"                        style={{ color: '#B2B3B9' }}                    >                        Оценка                        {sortBy === 'rating' && (                            <span className="ml-1" style={{ color: '#B2B3B9' }}>                                {sortOrder === 'asc' ? '↑' : '↓'}                            </span>                        )}                    </button>                </th>                <th className="w-[10%] text-center">                    <button                        onClick={() => onSort('price')}                        className="flex items-center justify-center gap-1 hover:text-gray-700 w-full"                        style={{ color: '#B2B3B9' }}                    >                        Цена, ₽                        {sortBy === 'price' && (                            <span className="ml-1" style={{ color: '#B2B3B9' }}>                                {sortOrder === 'asc' ? '↑' : '↓'}                            </span>                        )}                    </button>                </th>                <th className="w-[200px]"></th>            </tr>        </thead>    );};