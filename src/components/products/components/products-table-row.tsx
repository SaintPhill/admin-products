import type { Product } from '../../../types/product.ts';import { PriceFormatter } from './price-formatter.tsx';import { ActionButtons } from './action-buttons.tsx';interface ProductsTableRowProps {    product: Product;    isSelected: boolean;    onSelect: (id: number) => void;    onEdit: (product: Product) => void;    onMenu: (id: number) => void;    getRatingColor: (rating: number) => string;}export const ProductsTableRow = ({    product,    isSelected,    onSelect,    onEdit,    onMenu,    getRatingColor,}: ProductsTableRowProps) => {    return (        <tr className="hover:bg-gray-50 transition-colors" style={{ height: '71px' }}>            <td className={`w-[60px] ${isSelected ? 'border-l-2 border-l-[#3C538E]' : ''}`}>                <div className="flex items-center justify-center">                    <div                        onClick={() => onSelect(product.id)}                        className={`w-[22px] h-[22px] rounded cursor-pointer flex items-center justify-center ${                            isSelected ? 'bg-[#3C538E]' : 'border-2 border-gray-300 bg-white'                        }`}                    >                        {isSelected && (                            <svg                                className="w-[14px] h-[14px] text-white"                                fill="none"                                viewBox="0 0 24 24"                                stroke="currentColor"                                strokeWidth="3"                            >                                <path                                    strokeLinecap="round"                                    strokeLinejoin="round"                                    d="M5 13l4 4L19 7"                                />                            </svg>                        )}                    </div>                </div>            </td>            <td className="w-[calc(40%-60px)] text-left">                <div className="flex items-center gap-3">                    <div className="w-12 h-12 bg-[#C4C4C4] rounded-lg flex-shrink-0"></div>                    <div className="min-w-0 flex-1">                        <p                            className="truncate max-w-[310px]"                            style={{ fontWeight: 'bold', fontSize: '16px', color: '#1F2937' }}                        >                            {product.title}                        </p>                        <p className="text-sm text-gray-500 truncate max-w-[410px]">                            {product.description?.substring(0, 100) || 'Нет описания'}                        </p>                    </div>                </div>            </td>            <td                className="w-[15%] text-center truncate"                style={{ fontWeight: 'bold', fontSize: '16px', color: '#1F2937' }}            >                {product.brand || '-'}            </td>            <td className="w-[15%] text-center truncate">{product.sku || '-'}</td>            <td className="w-[10%] text-center">                <span className={getRatingColor(product.rating)}>                    {product.rating?.toFixed(1) || '-'}                </span>            </td>            <td className="w-[10%] text-center">                <PriceFormatter price={product.price} />            </td>            <td className="w-[200px]">                <ActionButtons onEdit={() => onEdit(product)} onMenu={() => onMenu(product.id)} />            </td>        </tr>    );};