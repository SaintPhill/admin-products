import { ChevronLeft, ChevronRight } from 'lucide-react';import { getPageNumbers } from '../../../utils/get-page-numbers.ts';interface PaginationProps {    currentPage: number;    totalPages: number;    totalItems: number;    startIndex: number;    endIndex: number;    onPageChange: (page: number) => void;    onPrevPage: () => void;    onNextPage: () => void;}export const Pagination = ({    currentPage,    totalPages,    totalItems,    startIndex,    endIndex,    onPageChange,    onPrevPage,    onNextPage,}: PaginationProps) => {    if (totalItems === 0) return null;    const pageNumbers = getPageNumbers(currentPage, totalPages);    return (        <div className="flex items-center justify-between py-[30px] border-t border-gray-200">            <div className="text-sm">                <span style={{ color: '#9CA3AF' }}>Показано </span>                <span style={{ color: '#1F2937' }}>                    {startIndex + 1}-{endIndex}                </span>                <span style={{ color: '#9CA3AF' }}> из </span>                <span style={{ color: '#1F2937' }}>{totalItems}</span>            </div>            <div className="flex items-center gap-2">                <button                    onClick={onPrevPage}                    disabled={currentPage === 1}                    className="w-[30px] h-[30px] flex items-center justify-center rounded bg-transparent hover:bg-gray-50 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"                >                    <ChevronLeft className="w-4 h-4" style={{ color: '#D1D5DB' }} />                </button>                {pageNumbers.map((page, index) => (                    <button                        key={index}                        onClick={() => typeof page === 'number' && onPageChange(page)}                        disabled={page === '...'}                        className={`w-[30px] h-[30px] flex items-center justify-center rounded text-sm transition-colors ${                            page === currentPage                                ? 'bg-[#797FEA] text-white font-medium'                                : page === '...'                                  ? 'border-none cursor-default text-gray-400'                                  : 'border border-gray-300 bg-white text-gray-400 hover:bg-gray-50 font-normal'                        }`}                        style={page === '...' ? { cursor: 'default' } : {}}                    >                        {page}                    </button>                ))}                <button                    onClick={onNextPage}                    disabled={currentPage === totalPages}                    className="w-[30px] h-[30px] flex items-center justify-center rounded bg-transparent hover:bg-gray-50 disabled:opacity-50 disabled:cursor-not-allowed transition-colors"                >                    <ChevronRight className="w-4 h-4" style={{ color: '#D1D5DB' }} />                </button>            </div>        </div>    );};