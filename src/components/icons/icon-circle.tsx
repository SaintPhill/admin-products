import * as React from 'react';import { cn } from '../../lib/utils.ts';type IconCircleProps = {    children: React.ReactNode;    outerBg?: string;    innerBg?: string;    className?: string;};export const IconCircle = React.forwardRef<HTMLDivElement, IconCircleProps>(    ({ children, outerBg = 'bg-white', innerBg = 'bg-white', className }, ref) => (        <div            ref={ref}            className={cn(                'relative flex items-center justify-center',                'w-[52px] h-[52px] rounded-full',                'shadow-circle-double',                outerBg,                className            )}        >            <div                className={cn(                    'absolute inset-0 m-auto relative flex items-center justify-center',                    'w-[50px] h-[50px] rounded-full',                    innerBg,                    'shadow-inner-sm'                )}            >                {children}            </div>        </div>    ));IconCircle.displayName = 'IconCircle';