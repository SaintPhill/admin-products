import React from 'react';interface CheckboxFieldProps {    checked: boolean;    onChange: (e: React.ChangeEvent<HTMLInputElement>) => void;    label: string;}export const CheckboxField = ({ checked, onChange, label }: CheckboxFieldProps) => {    return (        <label className="flex items-center gap-3 cursor-pointer">            <div className="relative flex items-center justify-center">                <input                    type="checkbox"                    checked={checked}                    onChange={onChange}                    className="appearance-none w-[18px] h-[18px] rounded border-2 border-[#EDEDED] checked:bg-[#242EDB] checked:border-[#242EDB] focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-0 cursor-pointer m-0"                />            </div>            <span className="font-inter font-medium text-[16px] text-[#9C9C9C] leading-none">                {label}            </span>        </label>    );};