import { RefreshCw, Plus } from 'lucide-react';import { Button } from '../../../components/ui/button.tsx';interface ProductsToolbarProps {    onRefresh: () => void;    onAdd: () => void;    isLoading?: boolean;}export const Toolbar = ({ onRefresh, onAdd, isLoading }: ProductsToolbarProps) => {    return (        <div className="p-[30px] border-gray-200">            <div className="flex items-center justify-between">                <div className="text-lg font-medium text-gray-900">Все позиции</div>                <div className="flex items-center gap-3">                    <Button                        onClick={onRefresh}                        variant="outline"                        size="sm"                        disabled={isLoading}                        className="w-[42px] h-[42px] p-0 border-2 border-[#ECECEB]"                    >                        <RefreshCw className={`h-5 w-5 ${isLoading ? 'animate-spin' : ''}`} />                    </Button>                    <Button                        onClick={onAdd}                        className="w-[147px] h-[42px] bg-[#242EDB] hover:bg-[#1a22b5] text-white flex items-center justify-center gap-2"                    >                        <div className="w-[22px] h-[22px] rounded-full border-2 border-white flex items-center justify-center">                            <Plus className="h-[14px] w-[14px] text-white" strokeWidth={3} />                        </div>                        <span                            className="font-semi-bold text-[14px]"                            style={{ fontFamily: 'Cairo, sans-serif', fontWeight: 600 }}                        >                            Добавить                        </span>                    </Button>                </div>            </div>        </div>    );};